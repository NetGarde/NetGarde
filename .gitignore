# Python cache (global)
__pycache__/
*.pyc

# Virtualenvs
.venv/
venv/

# Environment files (global)
.env
.env.*
!.env.example
!.env.production.example
!.env.dev.example
!aws/.env.example
!frontend/.env.development.example

# Production secrets (keep server-local; do not track in git)
backend/.env.production

# Node
node_modules/

# GitHub
.github/secrets

# Private keys (never commit; use Secrets Manager / ~/.ssh)
*.pem
.DS_Store

# TrustTwin (separate repo — clone as sibling for local API builds)
# trusttwin/data/

